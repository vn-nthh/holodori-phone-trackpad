The PC touch overlay is now off by default so it stays out of the way while you play. Use the **with Touch Overlay** shortcut or `--overlay` when you want the fingertip view. The Android companion is updated to 0.1.2.
