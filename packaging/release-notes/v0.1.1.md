The Windows installer now includes UsbDk, so there is no separate USB driver download. Existing UsbDk installations are detected automatically and left installed when Holodori is removed. The Android companion is updated to 0.1.1, and the portable Windows app remains available.
