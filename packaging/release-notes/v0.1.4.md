Touches just outside the play zone now map to the nearest lane, and the AOA and ADB editors behave the same way. Connection Doctor adds clear error codes and safe retry steps. ADB transitions and cleanup are also safer; install both updated apps for the full fix.
