Long sessions no longer show steadily increasing benchmark latency. Starting a new PC session now clears stale phone input and safely releases held keys. Install both the 0.2.0 Windows and Android apps because this version updates the touch protocol.
