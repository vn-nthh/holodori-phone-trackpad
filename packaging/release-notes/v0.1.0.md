Holodori now uses Android Open Accessory by default, so USB debugging is not required. This release includes the Windows app, Android companion, and an experimental touch overlay. Legacy ADB mode remains available.
